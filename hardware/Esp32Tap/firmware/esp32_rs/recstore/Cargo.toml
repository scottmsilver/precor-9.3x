[package]
name = "recstore"
version = "0.0.0"
edition = "2021"
publish = false

# NO DEPENDENCIES, same discipline as safety_core / reqbudget / program_core:
# the record format and ring logic are pure, so they test on the host in
# milliseconds instead of needing an Xtensa image and a QEMU boot. Flash I/O is
# behind the `Flash` trait, which the firmware implements over esp_partition.
[dependencies]
