[project]
name = "precor-treadmill"
version = "0.1.0"
requires-python = ">=3.9"

[tool.pytest.ini_options]
asyncio_mode = "auto"
markers = [
    "hardware: marks tests that require Pi hardware (deselect with '-m \"not hardware\"')",
]
